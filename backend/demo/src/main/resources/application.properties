spring.application.name=demo

# MySQL connection - Added allowPublicKeyRetrieval=true
spring.datasource.url=jdbc:mysql://localhost:3306/it342_lab1?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=CokeSpriteRoyal15

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Updated to the modern Dialect to avoid the WARN message
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=8081